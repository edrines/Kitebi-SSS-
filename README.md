# Kitebi SSS

Modern school information system for registration, data entry, search and retrieval.

## First version

The initial interface is a static prototype designed for GitHub Pages. It demonstrates the dashboard, registration, search and data-entry flows using browser-local demo data.

> Important: GitHub Pages is static hosting. A production system with shared student records, authentication, permissions and persistent school data will require a backend/database in a later phase.
